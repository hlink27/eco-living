<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Acompanhamento de Serviços</title>
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:opsz,wght@6..12,400;6..12,500;6..12,600;6..12,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
    <div class="wrapper">
      <div class="card">
        <header class="main-header">
          <div class="brand">
            <h1 class="name"><%= pageTitle %></h1>
          </div>
        </header>
        <main class="main-content">
          <div class="scrolling-container">
            <% if(unidadeBool){ %>
              <form class="input-form" action="/add-unidade" method="POST" enctype="multipart/form-data" autocomplete="off">
                <h2>Título:</h2>
                <input type="text" name="nome" required>
                <h2>Imagem:</h2>
                <input type="file" name="imagem" required>
                <br><br><br><br><br>
                <input type="submit">
              </form>
            <% } else if(temaBool){ %>
              <form class="input-form" action="/add-tema" method="POST">
                <h2>Título:</h2>
                <input type="text" name="nome" required>
                <input type="hidden" name="unidadeId" value="<%= unidade.id %>">
                <br><br><br><br><br>
                <input type="submit">
              </form>
            <% } else { %>

            <% }%>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>